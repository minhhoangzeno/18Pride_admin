// import { useToasts } from "react-toast-notifications"

// export default function addToastNotification(message, status) {
//     const { addToast } = useToasts();
//     return addToast(message, { appearance: status })
// }